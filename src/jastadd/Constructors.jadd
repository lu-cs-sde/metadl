aspect Constructors {
	// IntConstant.IntConstant(String s) {
	// 	this(new Opt(), s);
	// }

	// StringConstant.StringConstant(String s) {
	// 	this(new Opt(), s);
	// }

	// Variable.Variable(String s) {
	// 	this(new Opt(), s);
	// }

	// Atom.Atom(PredicateSymbol p, List<Term> ts) {
	// 	this(new Opt(), p, ts);
	// }

	// ListType.ListType(Type t) {
	// 	this(new Opt(), t);
	// }
}
