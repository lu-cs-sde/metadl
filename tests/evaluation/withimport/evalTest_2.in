IMPORT("./tests/evaluation/withimport/input_1.in", 'Program1).

analyze ('Program1) {
	Arity($i) :- [$p(...,$i:$v).].
}

OUTPUT('Arity).

analyze ('Program1) {
	MoreThan3(v) :- [$p($v1, $v2, ..., $i:$v).], BIND(v, $i + 1).
}

OUTPUT('MoreThan3).
