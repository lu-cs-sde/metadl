IMPORT('P, "extension/javafiles", "*.java").

P("", "") :- P("", "").

java('P) {
    myLabel :: <: `x + 1 :> ~ <: 1 + `x :>.
    NewArray(l, c, file) :- e <: new `t[] { .. } :>, SRC(e, l, c, _, _, file).
}

