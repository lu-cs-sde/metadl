IMPORT('P, "extension/javafiles", "*.java").

P("", "") :- P("", "").

java('P) {
    myLabel :: <: `x + 1 :> ~ <: 1 + `x :>.
    # myLabel :: <: `x + `y :> ~ <: `y + `x :>.

    NewArray(l, c, file) :- myLabel : <: new `t[] { .. } :>, SRC(e, l, c, _, _, file).
    Q(0) :- Q(0).
}
