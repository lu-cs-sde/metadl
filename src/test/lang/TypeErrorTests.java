package lang;

public class TypeErrorTests {
	
	/*
	@Test
	public void testTypeIds() {
		StringType st = StringType.type();
		assertTrue(st == StringType.type());
		
		IntegerType it = IntegerType.type();
		assertTrue(it == IntegerType.type());
		
		PredRefType pt = PredRefType.type();
		assertTrue(pt == PredRefType.type());
		
		VariableType vt = VariableType.type();
		assertTrue(vt == VariableType.type());
		
		ListType lt = ListType.type();
		assertTrue(lt == ListType.type());
		
		TreeSet<Type> types = new TreeSet<>();
		types.add(st); types.add(it); types.add(pt); types.add(vt); types.add(lt);
		
		assertTrue(types.size() == 5);
		assertTrue(st.isString());
		assertTrue(it.isInteger());
		assertTrue(pt.isPredRef());
		assertTrue(vt.isVariable());
		assertTrue(lt.isList());
		System.out.println(types);
	}
	 */
}
