OUTPUT(x) :- ATOM(x).
TYPEOF('C, [Integer String]).
TYPEOF('B, [String Integer]).

A(x, y) :- B(y, x), C(x, y).
E(x, y, 'B) :- B(y, x), C(x, y).
E(x, y, z) :- C(x, f1), 
              C(y, f2),
              BIND(z, x + y). 

F(x) :- BIND(x, 0).
